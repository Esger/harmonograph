# Harmonograph

An interactive harmonograph visualizer.

## Development

To run the project locally with hot-reloading:

```bash
npm install
npm run dev
```

## Build

To create a production build in the `dist/` folder:

```bash
npm run build
```
